// Package cache provides a file-backed, TTL-based cache for Slack entity lists (channels and users). Each entry is stored as a JSON file under os.UserCacheDir()/slackseek/{workspaceKey}/.
package cache
